# Restyle defaults, complete thumbnails, and reference uploads

Approved implementation in the canonical public Desktop checkout.

- Restyle retains its No style start. Choosing a style defaults to Attach when an executable native multi-image route can carry source and style; otherwise Describe with a visibly disabled Attach and explanation.
- Share capability resolution between UI and submission. Reserve a native style slot, preserve the source, expose overflow, and combine style/source references on supported OpenAI, FAL and CivitAI paths with truthful persisted provenance.
- Fit complete images into existing Desktop thumbnail cells, including rounded/circular badges and Canvas filmstrip cells; preserve source bytes, caches, temporal geometry and explicit crop/zoom workflows.
- Add Upload Images to the shared reference picker, reusing the existing importer. Imports appear selected immediately; Attach confirms the selection. Cancel retains imported library media without applying reference changes. Preserve empty-library access, ordering, deduplication, progress and errors.
- Keep existing persisted request compatibility; no global preference or database migration. Validate offline reference payloads/provenance with unrelated fixtures, native thumbnail and upload behavior, Swift build/tests, source hygiene, whitespace and REUSE when available. No paid provider calls, commit, publication or pin advancement.
