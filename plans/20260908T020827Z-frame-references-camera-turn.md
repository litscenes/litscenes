# Reliable Frame References and Turn Camera Controls

Approved implementation in the canonical public Desktop checkout. Keep changes uncommitted and unpublished and preserve the private submodule pin.

## Reframe

Keep the existing right sidebar, bound its content to the image viewport, scroll the body and pin Generate plus its prerequisite guidance. Use explicit dark text/caret on light editor backgrounds. Rename Viewpoint to Turn Camera for new requests. The clicked point is a stationary camera position, not an orbit target. Use a draggable position marker without zoom resize/roll handles. Turn ranges from -180 to 180 degrees and tilt from -90 to 90, at 5-degree steps, default zero. Turn presets: -90, -45, 0, 45, 90; tilt presets: -45, 0, 45. Camera-guide and optional character-perspective text context live under Advanced.

Controls own camera geometry. Additional direction is editable, composed prompt inspectable. Zoom modes retain their editable prompts. One normalized camera intent drives prompts, attachment descriptions, map and provenance. Place the camera at the selected point, preserve that position while turning and tilting relative to the source forward direction; preserve the world and visible identities, infer newly visible surroundings coherently. Angles are requested AI direction, not measured reconstruction. Mandatory instructions precede optional notes/context under provider limits; no added inference call. Save outgoing mode drafts before switching. New optional camera-turn payload persists yaw, pitch and notes; legacy Viewpoint records and custom templates keep their meanings. Use a separate template family for new camera turns and existing executable provider capability wiring.

## Suggestion references

Resolve structured character identity/scene cast plus explicit mentions, with readable active sheet, composite, then source-photo priority. Deduplicate and honor provider capacity. One resolution supplies both suggestion surfaces, one-click rendering and editable Art-direct seeds. Explicit draft removals win. Show actual selected references and missing/omitted warnings with repair navigation. The top surface is labeled as featuring the same suggestion; retain existing first-Scene transition and in-place fulfillment.

## Validation

Verify native UI at normal/short window sizes and both appearances, edge pivots, signs/presets/map consistency, mode switching, persisted legacy data, reference fallback/capacity/removal, and a materially different inference counter-fixture. Scan changed production files for motivating fixture terms. Run Swift build/tests and hygiene checks. Verify safe success/failure/cancellation provenance in the shared Traces app using local mocks without paid provider calls. Update plan, logs, thoughts and human requirements in the relevant repositories.
