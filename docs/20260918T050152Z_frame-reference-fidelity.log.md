# Frame reference fidelity

2026-09-18T05:01:52Z Approved plan saved. Implement in the canonical public checkout, preserving existing narration work. Defaults are scoped to Restyle after style selection. Thumbnail dimensions stay fixed; the reference modal imports and selects images before Attach confirmation.

Implemented:
- Shared internal style-mode and native-capacity resolver, explicit disabled Attach explanations, one reserved style slot and source-first content priority. OpenAI/FAL/CivitAI native edit paths compose style and source together; CivitAI includes the style preamble and retains its actual returned recipe. Creative mode persists separately from FAL edit-endpoint selection.
- Shared fitted thumbnail presentation across Desktop media/frame/reference/style surfaces, inscribed rectangular content inside circular badges, complete Canvas timeline samples and independently fitted stored scrub-strip samples. Source bytes and existing thumbnail caches are retained.
- Reusable optional MediaPickerSheet upload callback, wired to the existing Frame Creator importer. Imports are merged/deduplicated locally and selected immediately; confirmation still applies the reference selection. Empty libraries can open the picker.

Validation:
- Native SwiftUI image rendering visually confirms all four corners for portrait, square, landscape and panoramic fixtures in rectangular/circular wells and all five filmstrip samples.
- Two required inference contract checks cover capable/unsupported models, explicit choices, source retention, native limits, unrelated project fixtures, CivitAI edit payloads, persisted creative mode, and successful/canceled local trace records readable through the same trace reader as Logs. No provider requests were sent.
- The motivating screenshot's distinctive subject/style phrases have no matches in changed production Swift source. Existing provider/product names identify executable integrations, not fixture behavior. Pre-existing narration source was preserved.
- An initial full suite passed all 1,325 tests. The final incremental verification encountered disk exhaustion during ad-hoc signing; only this task's temporary SDK compiler files and incomplete executable copy were removed to recover space. Final result recorded below.
- The native file chooser was not exercised against a live project. Import cancellation/deduplication/persistence use the existing importer; picker state and confirmation behavior were inspected directly. REUSE CLI is not installed.

2026-09-18T05:24:51Z frame-reference-fidelity: completed the approved public working-tree implementation. Final swift build and all 1,325 tests pass, both public/private source-hygiene checks pass (including the pinned consumer hygiene scan), git diff --check passes, and native fitted-thumbnail rendering was visually reviewed. Local ad-hoc signing recovered after freeing only task-created temporary compiler files; disk remains nearly full. No paid provider calls, commits, pushes, or submodule changes. Commit reference pending explicit authorization; REUSE CLI unavailable.

Native fitting review artifact: `/private/tmp/litscenes-thumbnail-review.png`. Final build/test transcripts: `/private/tmp/litscenes-reference-build.log`, `/private/tmp/litscenes-reference-tests.log`.
