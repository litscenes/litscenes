# Narration clipboard compatibility

2026-09-18T17:43:03Z Read-only diagnosis confirmed a narration audio-region payload, including a valid media reference but no whole-take representation or legacy source project ID. Existing Copy has no success acknowledgement. Preserve all unrelated public edits and the separate running application.

2026-09-18T17:56:07Z Completed dual-format timeline Copy and Cut, direct and keyboard verification, visible copy acknowledgement, shared clipboard revision observation, and unified row paste availability with refusal reasons. Whole-take selection uses source Shot, exact media path and saved artifact identity when present; legacy regions without take/project IDs resolve in their current source project. No take provenance is inferred from labels or scripts. The timeline keeps its region payload and edits; Shot-row paste retains the established whole-take-at-zero contract.

Validation: existing `swift test --disable-automatic-resolution --no-parallel` passed all 1,325 tests. A disposable native diagnostic under `.build` linked the public app's real code and passed 26 checks through a separate NSPasteboard and generated local WAV media: direct/provider copies, both representations, legacy resolution, incompatible/missing media, whole-take and region timing, empty/populated/cross-project destinations, independent repeated copies, undo/redo, decodable full audio, failure acknowledgement, and Cut deferral. The user's general clipboard was unchanged. No new tracked tests were added.

The development bundle was rebuilt at `dist/LitScenes Development.app`; source hygiene, diff whitespace and code signature checks passed. Both clipboard UTIs are present. The packaged executable's code hash and Mach-O UUID match the public build (whole-file hashes differ because app signing changes signature bytes). REUSE CLI is unavailable. The app was not launched, and no paid calls or user-project mutations were used for validation. The separately running app remains untouched; use this new public bundle to receive the fix.

Prior public app and iconset are retained at `dist/.deprecated_LitScenes_Development.app_20260918T175453Z` and `dist/.deprecated_LitScenes_Development.iconset_20260918T175453Z`; these backups may be deleted after review. Unrelated concurrent edits are preserved. No commits, branches, pushes, private checkout changes, or publication were performed; no implementation commit hash exists.
