# Shot state preservation

Implemented the approved plan in the canonical public checkout, preserving unrelated unstaged work.

- Canonical saved timeline returned to all direct engine save callers; compound writes publish normalized inputs.
- Meaningful references retained by normalization; AI identity survives the actual application JSON codec.
- Document v0.6 performs deterministic legacy recovery before normal editing. Required orphan ancestors regain their placements; source-matching old continuations become alternate takes without switching the current selection. Verified continuation-only history IDs affect presentation only, retaining immutable render artifacts.
- Attempt saves gate provider execution; final save failure cannot report success. Completed media has a local recovery receipt consumed by the existing free repair action; canonical inference tracing remains in place.
- Combined source boundaries are excluded from pending ending inference.

Validation used isolated diagnostics outside the repository; no test files were added or edited. The actual compiled app module and real ProjectContextStore/SQLite path were used, with app support redirected to a temporary root. Live project data and media were read only.

The copied affected project produces three clips, 24.7917 seconds, with the two-model provenance and the old extension behind Takes. Three save/reopen cycles produce the identical canonical document and ordered media. Fresh state transitions cover initial saved generation, append, pending/ready ending, additional continuation, earlier and tail retakes, removal, undo, skip, failed/canceled/interrupted attempts, branching and combination. An injected SQLite write failure leaves the prior document intact. Unknown future schema versions remain unchanged. Combining two complete sequences retains six clips.

Real AVFoundation composition and MP4 export both report 24.7917 seconds. Offscreen native row/modal snapshots were inspected; the row has three positions and two continuation takes, and the player timeline has the full three-clip sequence. Accessibility is disabled, so clicks in the running operator app were not automated. No live paid generation was issued; provider execution is not claimed as tested.

All 1,275 existing tests pass. Source hygiene and diff checks pass. Build/package/signature results are recorded in LOG.md. REUSE is unavailable locally. No commits, pushes, branches, worktrees or remote schema changes.
